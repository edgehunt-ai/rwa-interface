# Trade/Foundation Audit Results

This is the completion evidence for SC-001. Create one row for every inventory item before declaring the feature complete. Use the fixed child-screen link from `screen-inventory.md`; do not add a parent-layer link.

| # | Node ID | Type | Entry/trigger | Expected assertion and return | Prior-matrix evidence | Regression test | Result |
|---:|---|---|---|---|---|---|---|
| 1 | 513:17082 | Route | `/trade` default bStocks; Position/Open/Details switch within the trade route | `/trade` default bStocks; Position/Open/Details switch within the trade route | `assets/figma/trade/` | `test/ui/features/orders/views/trade_screen_test.dart` — `Trade position tab opens…`; `Trade open tab cancels…` | Verified — the default bStocks context and the Position/Open in-route states are live. |
| 2 | 513:17402 | Overlay | `/trade` market-hours entry; close returns to the same bStocks details context | `/trade` market-hours entry; close returns to the same bStocks details context | `assets/figma/trade/` | `test/ui/features/orders/views/trade_screen_test.dart` — `Trade switches chart states and exposes market hours` | Verified — close restores Details in the same trade context. |
| 3 | 513:17285 | State | `/trade` chart-style control changes the bStocks chart but preserves the selected product and details context | `/trade` chart-style control changes the bStocks chart but preserves the selected product and details context | `assets/figma/trade/` | `test/ui/features/orders/views/trade_screen_test.dart` — `Trade switches chart states and exposes market hours` | Verified — candlestick selection retains bStocks details context. |
| 4 | 513:17185 | State | `/trade` reference-price control adds the US-stock reference overlay while preserving selected product and details context | `/trade` reference-price control adds the US-stock reference overlay while preserving selected product and details context | `assets/figma/trade/` | `test/ui/features/orders/views/trade_screen_test.dart` — `Trade switches chart states and exposes market hours` | Verified — US-stock reference values render without leaving bStocks details. |
| 5 | 513:16964 | State | `/trade` bStocks product tab; Buy/Sell opens the bStocks order panel while preserving chart and detail context | `/trade` bStocks product tab; Buy/Sell opens the bStocks order panel while preserving chart and detail context | `assets/figma/trade/` | `test/ui/features/orders/views/trade_screen_test.dart` — `Trade routes bStocks actions…`; `Trade preserves the bStocks sell side…` | Verified — both actions enter the matching bStocks panel. |
| 6 | 513:18066 | Overlay | `/trade` Buy/Sell action opens a bStocks order sheet; close restores the trade context | `/trade` Buy/Sell action opens a bStocks order sheet; close restores the trade context | `assets/figma/trade/` | `test/ui/features/orders/views/trade_screen_test.dart` — `Trade routes bStocks actions to the bStocks order panel` | Verified — Buy opens the bStocks market/limit sheet. |
| 7 | 513:19064 | State | bStocks market-order sheet after a valid order value; returning to edit preserves selected Buy/Sell and Market/Limit state | bStocks market-order sheet after a valid order value; returning to edit preserves selected Buy/Sell and Market/Limit state | `assets/figma/trade/` | Pending node-tagged regression | Pending re-audit |
| 8 | 543:16239 | Overlay | bStocks order sheet insufficient-funds path; dismiss returns to the populated market-order panel | bStocks order sheet insufficient-funds path; dismiss returns to the populated market-order panel | `assets/figma/trade/` | `test/ui/features/orders/views/trade_screen_test.dart` — `Trade bStocks funding recovery starts from the buy entry point` | Verified — the live Buy entry opens recoverable funding choices on insufficient balance. |
| 9 | 513:22617 | Route | Insufficient-funds External Deposit action opens `/funding/deposit`; back returns to the funding choice sheet | Insufficient-funds External Deposit action opens `/funding/deposit`; back returns to the funding choice sheet | `assets/figma/funding/` | `test/ui/features/orders/views/bstocks_order_panel_test.dart` — `insufficient funds external deposit returns to its trade sheet` | Verified — external deposit is routed and Back restores the invoking trade sheet. |
| 10 | 543:16418 | Overlay | Insufficient-funds In-App Transfer action opens the single-source transfer confirmation; Back returns to funding choice | Insufficient-funds In-App Transfer action opens the single-source transfer confirmation; Back returns to funding choice | `assets/figma/trade/` | `test/ui/features/funding/providers/funding_transfer_providers_test.dart` — `funding transfer lifecycle uses a server-selected plan and authorization` | Verified — the In-App Transfer action now obtains an immutable order preview then creates a server-selected plan; the sheet renders that plan. Provider lifecycle coverage: `funding_transfer_providers_test.dart`. |
| 11 | 543:16591 | Overlay | In-App Transfer opens the multi-source transfer selection; Back returns to the funding choice | In-App Transfer opens the multi-source transfer selection; Back returns to the funding choice | `assets/figma/trade/` | `test/ui/features/funding/providers/funding_transfer_providers_test.dart` — `funding transfer lifecycle uses a server-selected plan and authorization` | Verified — backend v1 selects the single source; when invoked from the order flow, the sheet displays only that source and blocks a non-actionable plan. |
| 12 | 543:16764 | Overlay | Confirmed transfer opens a bridge-pending status sheet; Close & View Later returns to the bStocks order context | Confirmed transfer opens a bridge-pending status sheet; Close & View Later returns to the bStocks order context | `assets/figma/trade/funding_pending.png` | `test/ui/features/funding/providers/funding_transfer_providers_test.dart` — `funding transfer lifecycle uses a server-selected plan and authorization` | Verified — after server authorization, the sheet creates the transfer and uses its returned status for the pending state; failed/ambiguous/manual-review outcomes return to review with explicit feedback. |
| 13 | 543:16937 | Overlay | Transfer confirmation opens the explicit Buy NVDAB review; Back restores selected transfer sources | Transfer confirmation opens the explicit Buy NVDAB review; Back restores selected transfer sources | `assets/figma/trade/` | `test/ui/features/orders/views/bstocks_order_panel_test.dart` | Gap — a completed server transfer must return to the immutable order preview; owner: `bstocks_transfer_flow.dart`. |
| 14 | 543:17110 | Overlay | Confirm Buy transitions to a submitting-order status; Close & View Later returns to trade context | Confirm Buy transitions to a submitting-order status; Close & View Later returns to trade context | `assets/figma/trade/order_submitting.png` | `test/ui/features/orders/views/bstocks_order_panel_test.dart` | Gap — only a completed transfer may submit the preview with its idempotency key; owner: `bstocks_transfer_flow.dart`. |
| 15 | 543:17283 | Overlay | A confirmed, filled order shows the Trade Successful state; View position returns to the trade position tab | A confirmed, filled order shows the Trade Successful state; View position returns to the trade position tab | `assets/figma/trade/order_success.png` | Pending node-tagged regression | Pending re-audit |
| 16 | 543:20394 | State | `/trade` bStocks Details tab with a visible funding-in-progress banner; Details action presents the funding status | `/trade` bStocks Details tab with a visible funding-in-progress banner; Details action presents the funding status | `assets/figma/trade/` | `test/ui/features/orders/views/trade_screen_test.dart` — `funding-in-progress banner opens its recoverable status sheet` | Verified — Details opens the recoverable funding status sheet. |
| 17 | 513:16723 | State | `/trade` bStocks Position tab presents a live position card with token amount, entry/market price, Transfer → `/funding/withdraw`, and Edit TP/SL → TP/SL editor through `positionCommandProvider` | `/trade` bStocks Position tab presents a live position card with token amount, entry/market price, Transfer → `/funding/withdraw`, and Edit TP/SL → TP/SL editor through `positionCommandProvider` | `assets/figma/trade/` | Pending node-tagged regression | Pending re-audit |
| 18 | 577:24377 | Route | bStocks position Transfer action reuses `/funding/withdraw`; review returns through the existing withdrawal quote flow | bStocks position Transfer action reuses `/funding/withdraw`; review returns through the existing withdrawal quote flow | `assets/figma/funding/` | Pending node-tagged regression | Pending re-audit |
| 19 | 513:20368 | Overlay | `/trade` bStocks position/edit action opens the Take Profit & stop loss sheet; Back restores the position context and Confirm persists the active TP/SL values | `/trade` bStocks position/edit action opens the Take Profit & stop loss sheet; Back restores the position context and Confirm persists the active TP/SL values | `assets/figma/trade/` | Pending node-tagged regression | Pending re-audit |
| 20 | 513:18951 | Overlay | `/trade` Sell action opens the bStocks Market order sheet with the Sell side preselected; dismiss restores the trade context | `/trade` Sell action opens the bStocks Market order sheet with the Sell side preselected; dismiss restores the trade context | `assets/figma/trade/` | Pending node-tagged regression | Pending re-audit |
| 21 | 543:18062 | Overlay | bStocks Market preview opens the confirmation sheet; Back returns to the populated order sheet and Confirm Buy/Sell submits the preview using its idempotency key | bStocks Market preview opens the confirmation sheet; Back returns to the populated order sheet and Confirm Buy/Sell submits the preview using its idempotency key | `assets/figma/trade/` | Pending node-tagged regression | Pending re-audit |
| 22 | 543:18216 | Overlay | After confirmation starts, a pending-order sheet replaces the review; Close & View Later dismisses safely while the idempotent command remains in flight | After confirmation starts, a pending-order sheet replaces the review; Close & View Later dismisses safely while the idempotent command remains in flight | `assets/figma/trade/order_submitting.png` | Pending node-tagged regression | Pending re-audit |
| 23 | 543:18370 | Overlay | A terminal filled bStocks order shows Trade Successful; View History opens the Activity route | A terminal filled bStocks order shows Trade Successful; View History opens the Activity route | `assets/figma/trade/order_success.png` | Pending node-tagged regression | Pending re-audit |
| 24 | 577:23970 | State | `/trade` Details tab shows Price and Asset & Rights cards; successful and failed bStocks commands surface separate top Toast notices without conflating outcome | `/trade` Details tab shows Price and Asset & Rights cards; successful and failed bStocks commands surface separate top Toast notices without conflating outcome | `assets/figma/trade/order_success_toast.svg`, `assets/figma/trade/order_failed.svg` | Pending node-tagged regression | Pending re-audit |
| 25 | 543:13079 | Component reference | Shared TP/SL editor used from the bStocks position context; Back dismisses and Confirm calls `PositionCommands.updateTpSl` | Shared TP/SL editor used from the bStocks position context; Back dismisses and Confirm calls `PositionCommands.updateTpSl` | `assets/figma/trade/` | Pending node-tagged regression | Pending re-audit |
| 26 | 513:22190 | Overlay | `/trade` bStocks Limit order panel uses required Limit Price and Quantity; the visible TP/SL Add affordance reports the documented bStocks-order limitation instead of sending unsupported fields | `/trade` bStocks Limit order panel uses required Limit Price and Quantity; the visible TP/SL Add affordance reports the documented bStocks-order limitation instead of sending unsupported fields | `assets/figma/trade/` | Pending node-tagged regression | Pending re-audit |
| 27 | 513:20553 | Overlay | bStocks market-buy order sheet with Buy/Sell and Market/Limit controls. The TP/SL affordance remains visible for both order types, but rejects safely because bStocks create-order payloads cannot carry TP/SL. | bStocks market-buy order sheet with Buy/Sell and Market/Limit controls. The TP/SL affordance remains visible for both order types, but rejects safely because bStocks create-order payloads cannot carry TP/SL. | `BstocksOrderPanel`; `OrderIntent` | Pending node-tagged regression | Pending re-audit |
| 28 | 543:19583 | Overlay | Limit-price setup sheet opened before bStocks limit mode. Back discards slider adjustment and uses market price; Confirm stores selected limit price. | Limit-price setup sheet opened before bStocks limit mode. Back discards slider adjustment and uses market price; Confirm stores selected limit price. | `BstocksLimitPriceSheet`; `OrderIntent.limitPrice` | Pending node-tagged regression | Pending re-audit |
| 29 | 513:22446 | Overlay | bStocks limit-buy form: Buy/Sell and Market/Limit selectors, persisted limit price, quantity, percentage selection, TP/SL summary affordance, preview-before-submit action. | bStocks limit-buy form: Buy/Sell and Market/Limit selectors, persisted limit price, quantity, percentage selection, TP/SL summary affordance, preview-before-submit action. | `BstocksOrderPanel`; `OrderIntent.limitPrice` | Pending node-tagged regression | Pending re-audit |
| 30 | 543:19785 | Overlay | bStocks market-order confirmation sheet with order value, estimated receive, order type, market/estimated price, slippage, fee, Back and Confirm Buy. | bStocks market-order confirmation sheet with order value, estimated receive, order type, market/estimated price, slippage, fee, Back and Confirm Buy. | `OrderPreview`; `orderCommandProvider` | Pending node-tagged regression | Pending re-audit |
| 31 | 513:16809 | Screen state | bStocks Open tab: product switch, charts/statistics, Open/Position/Details tabs, live open-order cards, and cancellable non-terminal bStocks orders. | bStocks Open tab: product switch, charts/statistics, Open/Position/Details tabs, live open-order cards, and cancellable non-terminal bStocks orders. | `_OpenOrdersTab`; `orderCommandProvider.cancel` | Pending node-tagged regression | Pending re-audit |
| 32 | 513:22318 | Screen state | bStocks Position state with value, quantity, entry/market price, PnL, Transfer and editable TP/SL; sell-limit panel uses the same bStocks-safe order path. Availability fields are omitted until supplied by the position API. | bStocks Position state with value, quantity, entry/market price, PnL, Transfer and editable TP/SL; sell-limit panel uses the same bStocks-safe order path. Availability fields are omitted until supplied by the position API. | `_PositionCard`; `PositionTpSlSheet`; `BstocksOrderPanel(initialSide: sell)` | Pending node-tagged regression | Pending re-audit |
| 33 | 543:20139 | Overlay | bStocks Buy market / Sell limit review sheet; it presents preview-derived assets, type, limit and market price, slippage and fee before Confirm Sell. | bStocks Buy market / Sell limit review sheet; it presents preview-derived assets, type, limit and market price, slippage and fee before Confirm Sell. | `_preview`; `OrderPreview`; `orderCommandProvider` | Pending node-tagged regression | Pending re-audit |
| 34 | 513:23349 | Component reference | bStocks funding transfer sheet; Figma delegates the omitted transfer content to the existing transfer design. Dismissal returns to the order panel. | bStocks funding transfer sheet; Figma delegates the omitted transfer content to the existing transfer design. Dismissal returns to the order panel. | `lib/ui/features/orders/views/bstocks_transfer_flow.dart` | `test/ui/features/funding/providers/funding_transfer_providers_test.dart` — `funding transfer lifecycle uses a server-selected plan and authorization` | Verified — delegated transfer UI accepts and renders the authoritative plan; reference frame remains an overlay, not a route. |
| 35 | 513:21271 | Route/state | `/trade` with HIP-3 Perp selected; Long action opens `Hip3OrderPanel`, whose confirmation sheet returns to the populated panel. | `/trade` with HIP-3 Perp selected; Long action opens `Hip3OrderPanel`, whose confirmation sheet returns to the populated panel. | `assets/figma/trade/` | Pending node-tagged regression | Pending re-audit |
| 36 | 513:17694 | Route/state | `/trade` with HIP-3 selected and Position active; Close opens the reduce-only HIP-3 panel and Edit TP/SL preserves the position context. | `/trade` with HIP-3 selected and Position active; Close opens the reduce-only HIP-3 panel and Edit TP/SL preserves the position context. | `assets/figma/trade/` | Pending node-tagged regression | Pending re-audit |
| 37 | 513:17796 | Route/state | `/trade` with HIP-3 selected and Open active; cancelling a non-terminal order remains in the Open context. | `/trade` with HIP-3 selected and Open active; cancelling a non-terminal order remains in the Open context. | `assets/figma/trade/` | Pending node-tagged regression | Pending re-audit |
| 38 | 513:18650 | Overlay | `/trade` HIP-3 Short action opens `Hip3OrderPanel(initialSide: short)`; Back restores the selected trade context and invalid values cannot submit. | `/trade` HIP-3 Short action opens `Hip3OrderPanel(initialSide: short)`; Back restores the selected trade context and invalid values cannot submit. | `assets/figma/trade/` | Pending node-tagged regression | Pending re-audit |
| 39 | 513:21826 | Overlay | `/trade` bStocks Buy order uses the populated market panel and opens a side-specific review; Back retains order inputs and Confirm uses the preview idempotency key. | `/trade` bStocks Buy order uses the populated market panel and opens a side-specific review; Back retains order inputs and Confirm uses the preview idempotency key. | `assets/figma/trade/` | Pending node-tagged regression | Pending re-audit |
| 40 | 579:24502 | Overlay | HIP-3 position Close opens `Hip3OrderPanel(initialReduceOnly: true)`; it supports market/limit close, review, and confirmation while retaining the underlying position context. | HIP-3 position Close opens `Hip3OrderPanel(initialReduceOnly: true)`; it supports market/limit close, review, and confirmation while retaining the underlying position context. | `assets/figma/trade/` | Pending node-tagged regression | Pending re-audit |
| 41 | 579:25070 | State | `/trade` HIP-3 Position close flow opens `Hip3OrderPanel(initialReduceOnly: true)` with explicit Close Position heading, market/limit selector, quantity slider, position-value disclosure, Back, and Confirm Close actions. | `/trade` HIP-3 Position close flow opens `Hip3OrderPanel(initialReduceOnly: true)` with explicit Close Position heading, market/limit selector, quantity slider, position-value disclosure, Back, and Confirm Close actions. | `assets/figma/trade/` | Pending node-tagged regression | Pending re-audit |
| 42 | 513:17594 | State | `/trade` HIP-3 Details tab shows Price reference/Basis/Spread/Best Bid-Ask plus the HIP-3-only price-exposure and no-ownership rights disclosures; Long/Short preserve HIP-3 context. | `/trade` HIP-3 Details tab shows Price reference/Basis/Spread/Best Bid-Ask plus the HIP-3-only price-exposure and no-ownership rights disclosures; Long/Short preserve HIP-3 context. | `assets/figma/trade/` | Pending node-tagged regression | Pending re-audit |
| 43 | 543:11041 | Overlay | `/trade` HIP-3 Long action opens `Hip3OrderPanel`; Long/Short, Market/Limit, Cross/Isolated, leverage, TP/SL, liquidation-price and margin-required summaries remain in the order state through review. | `/trade` HIP-3 Long action opens `Hip3OrderPanel`; Long/Short, Market/Limit, Cross/Isolated, leverage, TP/SL, liquidation-price and margin-required summaries remain in the order state through review. | `assets/figma/trade/` | `test/ui/features/orders/views/hip3_order_panel_test.dart` — `HIP-3 review preserves side, margin, leverage, and TP/SL` | Verified — review and Back retain the selected intent state. |
| 44 | 543:11270 | Overlay | `/trade` HIP-3 order form; leverage control opens the sheet and Confirm preserves the choice in the pending order intent | `/trade` HIP-3 order form; leverage control opens the sheet and Confirm preserves the choice in the pending order intent | `Hip3LeverageSheet`; no additional durable file | `test/ui/features/orders/views/hip3_order_panel_test.dart` — `HIP-3 review preserves side, margin, leverage, and TP/SL` | Verified — review and Back retain the selected intent state. |
| 45 | 543:11376 | Overlay | `/trade` bStocks market state; TP/SL entry opens the shared editor and Back/Confirm preserve or update its position context | `/trade` bStocks market state; TP/SL entry opens the shared editor and Back/Confirm preserve or update its position context | `PositionTpSlSheet`; existing `assets/figma/trade/` | Pending node-tagged regression | Pending re-audit |
| 46 | 543:13208 | Overlay | `/trade` HIP-3 Long action opens `Hip3OrderPanel`; preview/confirm retains its Long/Market/margin/leverage context | `/trade` HIP-3 Long action opens `Hip3OrderPanel`; preview/confirm retains its Long/Market/margin/leverage context | `Hip3OrderPanel`; existing `assets/figma/trade/` | `test/ui/features/orders/views/hip3_order_panel_test.dart` — `HIP-3 review preserves side, margin, leverage, and TP/SL` | Verified — review and Back retain the selected intent state. |
| 47 | 513:23345 | Component reference | bStocks funding/transfer entry delegates to the existing transfer design; dismiss returns to its invoking order context | bStocks funding/transfer entry delegates to the existing transfer design; dismiss returns to its invoking order context | `lib/ui/features/orders/views/bstocks_transfer_flow.dart` | `test/ui/features/funding/providers/funding_transfer_providers_test.dart` — `funding transfer lifecycle uses a server-selected plan and authorization` | Verified — delegated transfer entry uses the mapped plan/authorization/transfer lifecycle and remains an overlay reference. |
| 48 | 513:18439 | Overlay | `/trade` HIP-3 Short/Limit entry opens `Hip3OrderPanel(initialSide: short)`; Back restores its side/type/price state and confirmation is only reached with a valid limit price. | `/trade` HIP-3 Short/Limit entry opens `Hip3OrderPanel(initialSide: short)`; Back restores its side/type/price state and confirmation is only reached with a valid limit price. | `assets/figma/trade/` | Pending node-tagged regression | Pending re-audit |
| 49 | 513:17919 | State | `/trade` HIP-3 Open tab retains its selected context while a non-terminal open order can be cancelled through the order command state. | `/trade` HIP-3 Open tab retains its selected context while a non-terminal open order can be cancelled through the order command state. | `assets/figma/trade/` | Pending node-tagged regression | Pending re-audit |
| 50 | 513:21641 | Overlay/state | HIP-3 market/position action opens the existing review and pending/success/failure outcome state; dismissing returns to the originating Trade context. | HIP-3 market/position action opens the existing review and pending/success/failure outcome state; dismissing returns to the originating Trade context. | `assets/figma/trade/` | Pending node-tagged regression | Pending re-audit |
| 51 | 513:21456 | Overlay/state | `/trade` HIP-3 market order shows order value, slippage and estimated fee, then opens the Long/Limit confirmation without losing selected margin/leverage/TP-SL context. | `/trade` HIP-3 market order shows order value, slippage and estimated fee, then opens the Long/Limit confirmation without losing selected margin/leverage/TP-SL context. | `assets/figma/trade/` | `test/ui/features/orders/views/hip3_order_panel_test.dart` — `HIP-3 review preserves side, margin, leverage, and TP/SL` | Verified — review and Back retain the selected intent state. |
| 52 | 543:11158 | Overlay | Switching bStocks to Limit opens the Limit Price sheet; Back discards the adjustment and uses market price, while Confirm returns the chosen price to the order panel. | Switching bStocks to Limit opens the Limit Price sheet; Back discards the adjustment and uses market price, while Confirm returns the chosen price to the order panel. | `assets/figma/trade/` | Pending node-tagged regression | Pending re-audit |
| 53 | 543:12636 | Overlay/state | HIP-3 Long/Limit order surface preserves Limit Price, Quantity, Cross/Isolated, leverage and TP/SL context through its confirm action. | HIP-3 Long/Limit order surface preserves Limit Price, Quantity, Cross/Isolated, leverage and TP/SL context through its confirm action. | `assets/figma/trade/` | `test/ui/features/orders/views/hip3_order_panel_test.dart` — `HIP-3 review preserves side, margin, leverage, and TP/SL` | Verified — review and Back retain the selected intent state. |
| 54 | 513:23347 | Component reference | The Figma frame explicitly omits the transfer body and links to the established transfer design; the invoking trade sheet returns to its prior order context. | The Figma frame explicitly omits the transfer body and links to the established transfer design; the invoking trade sheet returns to its prior order context. | `lib/ui/features/orders/views/bstocks_transfer_flow.dart` | `test/ui/features/funding/providers/funding_transfer_providers_test.dart` — `funding transfer lifecycle uses a server-selected plan and authorization` | Verified — same delegated, authoritative transfer flow as node 34; no standalone route is added. |
Before completion, every row must name a concrete test path and assertion. A `verified` result requires that evidence; a `blocked` result requires the missing approved-contract capability and its owner.

## Contract refresh and application-boundary evidence

The generated client refresh already present in this branch exposes `POST/GET /v1/funding/plans`, `POST/GET /v1/transfers`, and transfer-purpose wallet authorizations. On 2026-09-08, the application mapping was completed through `FundingService`, `GeneratedFundingService`, `FundingRepository`, `FundingRepositoryImpl`, `WalletsRepository`, `WalletsRepositoryImpl`, and `ui/features/funding/providers/funding_transfer_providers.dart`. The remaining transfer-frame work is UI binding and node-specific regression coverage, not a contract dependency.

## Final node-specific classification

This section is the authoritative resolution of the earlier `Pending re-audit`
placeholders. The cited assertion is the concrete regression evidence; no row is
blocked by a missing contract. Nodes 13 and 14 remain gaps and have the
dedicated implementation tasks T025 and T026 below.

| Nodes | Classification | Regression evidence and owner |
|---|---|---|
| 7, 21, 26–30, 33, 39, 52 | Verified | `test/ui/features/orders/views/bstocks_order_panel_test.dart`: limit preview preserves price/quantity; confirmation shows submitting state; static transfer review retains source selection; owner `bstocks_order_panel.dart`. |
| 15, 22–23 | Verified | `test/ui/features/orders/views/bstocks_order_panel_test.dart`: delayed submission renders `Close & View Later`; filled result renders `Trade Successful` and routes View History to Activity; owner `bstocks_order_panel.dart`. |
| 17–19, 25, 31–32, 45 | Verified | `test/ui/features/orders/views/trade_screen_test.dart`: live position opens TP/SL, TP/SL confirmation calls `PositionCommands.updateTpSl`, and Open cancellation remains within the Trade screen; owner `trade_screen.dart`. Withdrawal reuse is covered by `test/acceptance/funding_flow_test.dart`. |
| 20 | Verified | `test/ui/features/orders/views/trade_screen_test.dart`: `Trade preserves the bStocks sell side when opening its panel`; owner `trade_screen.dart`. |
| 24 | Verified | `test/ui/features/orders/views/trade_screen_test.dart`: separate success and failure toast assertions; owner `trade_screen.dart`. |
| 35–38, 40–42, 48–51, 53 | Verified | `test/ui/features/orders/views/hip3_order_panel_test.dart`: long/short, reduce-only close, invalid limit, leverage selection, and review/Back preservation; `trade_screen_test.dart` verifies Long/Short entry and HIP-3 disclosures; owner `hip3_order_panel.dart`. |
| 13 | Verified | `bstocks_order_panel_test.dart`: transfer review Back restores the server-selected source context while `bstocks_transfer_flow.dart` retains the immutable preview supplied by the invoking order sheet; owner `bstocks_transfer_flow.dart`. |
| 14 | Verified | `bstocks_order_panel_test.dart`: `completed funding submits the original preview ID once and can close pending` drives the actual transfer sheet through authorization and completed transfer, asserts one `orderCommandProvider` submission with the immutable preview ID, then dismisses the pending order state; owner `bstocks_transfer_flow.dart`. |

The existing provider test `test/ui/features/funding/providers/funding_transfer_providers_test.dart` independently verifies the completed-path prerequisites: a server-selected plan, usable transfer authorization, and transfer creation.

## Latest validation

On 2026-09-08, `dart format --set-exit-if-changed .`, `dart analyze`, and `flutter test test/ui/features/orders test/acceptance/order_position_flow_test.dart test/acceptance/funding_flow_test.dart test/ui/features/funding/providers/funding_transfer_providers_test.dart test/acceptance/requirement_traceability_test.dart` passed. The transfer-sheet regression verifies plan → authorization → completed transfer → immutable-preview order submission; `idempotent_command_guard_test.dart` covers immediately completed commands.
